services:
  - type: web
    name: stock-monitor
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python monitor/web_monitor.py
    envVars:
      - key: PYTHON_VERSION
        value: "3.11"
